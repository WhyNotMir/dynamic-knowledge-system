# Secrets / env
.env
.env.local

# Runtime data
uploads/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Alembic autogenerated migrations — we commit them selectively, not in bulk
alembic/versions/*.py

# --- UI lives in its own tree, tracked separately ------------------------
dks-ui/

# Stray npm artefacts that landed in the repo root (leftover shadcn tooling)
node_modules/
package.json
package-lock.json

# Local tooling
.claude/
.DS_Store
