# Keep the Python image build context lean — the UI has its own Dockerfile,
# we don't want its node_modules bleeding into the FastAPI image.
uploads
__pycache__
*.pyc
.venv
.env
.git
.pytest_cache
node_modules
