# DKS UI

This is the frontend for DKS, built with Next.js.

The main project documentation lives in the repository root:
- [../README.md](../README.md)

## Local frontend-only development

```bash
cp .env.example .env.local
npm install
npm run dev
```

By default the UI expects the backend API at:

```text
http://localhost:8000
```
